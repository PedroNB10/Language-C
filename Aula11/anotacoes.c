#include <stdio.h>

//ponteiro guarda um endereço(posição) de memória

int *nome_do_ponteiro = NULL, variavel;

//tipo *nome_do_ponteiro

//o tipo de ponteiro aponta somente para o seu tipo de variavel

// ponteiros DEVEM SER INICIALIZADOS (ou apontando para NULL ou para uma variavel)
int a = 2;
int *x = &a;//o ponteiro recebe o endereço de memória da variável a; (OPERADOR UNÁRIO)

int main (){

    return 0;
}


