#include <stdio.h>

int main ()
{
    //fopen(nome,modoDeAbertura) abrir um arquivo, caso o arquivo não exista o arquivo é criado
    //fclose() utilizada para fechar um arquivo
    //fprintf() utilizada para escrever dentro de um arquivo de texto
    //fscanf() utilizada para ler o conteúdo de um arquivo de texto

    //para garantir os dados escritos sejam salvos lembre-se de colocar o fclose() no final
    //pode usar fflush para gravar dados

    //caso o arquivo exista e o método para abri-lo ser write, todo conteudo será perdido
    //modo de abertura:
    // write e read

    return 0;
}